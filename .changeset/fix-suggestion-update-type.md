---
'@lofcz/platejs-utils': patch
'@lofcz/platejs-suggestion': patch
---

Allow block-level `update` suggestions in `TSuggestionData` (prop-only container changes). Fixes the suggestion package typecheck that blocked the 53.4.1 release.
